// wengwengweng

use rlua::Lua;
use rlua::MetaMethod;
use rlua::UserData;
use rlua::UserDataMethods;
use rlua::Variadic;
use rlua::Function;
use rlua::Result;

fn main() -> Result<()> {

	return Ok(());

}

